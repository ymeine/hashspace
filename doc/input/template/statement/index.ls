i = require('importer').Importer module

export {
	name: '# template'
	category: 'Statement'

	catchPhrase: '''<code># template name(parameters)</code> ... <code># /template</code> is used to define a template.'''
	description: i.desc!

	example: i.ex!
	sample: i.sample!
}
