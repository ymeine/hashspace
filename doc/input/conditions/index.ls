i = require('importer').Importer module

module.exports = [
	{
		name: 'conditions'
		category: 'Statement'

		catchPhrase: '''You can conditionally execute parts of the template using either <code>{if}/{else if}/{else}</code> statements, or using a ternary expression.'''
		description: ''''''

		example: i.ex!
		sample: i.sample!
	}
]
