require! {
	node-path: path
}



export clearCache = -> for key, value of require.cache => delete require.cache[key]



# export class Loader
# 	(@module) ~>

# 	resolve: (path) ->
# 		@module.require.resolve path

# 	reload: (input) ->
# 		switch typeof! input
# 		| 'String'
# 			# resolved = @resolve input
# 			# delete @module.require.cache[resolved]
# 			# @module.require resolved
# 			require.cache = {}
# 			@module.require input
# 		| 'Array' => [@reload path for path in input]
# 		| 'Object'=> {[key, @reload path] for key, path of input}
