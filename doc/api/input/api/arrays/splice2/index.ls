i = require('importer').Importer module

export {
	name: 'Array.prototype.splice2(index, howMany, elements)'
	category: 'Core'

	catchPhrase: i.catch!
	description: i.desc!

	example: i.ex!
	sample: i.sample!
}
