{
	"title": "Array.prototype.splice2(index, howMany, elements)",

	"catchPhrase": "Equivalent of standard `splice` but using a list of items instead of relying on the variable length of the arguments list."
}
