require! {
	'entry'
}

{Entry} = entry





export apiToHTML = (api) ->
	entries = [new Entry input for input in api.entries]

	parts = for entry in entries
		entry.serialize 'html'
	output = parts.join '\n'

	output
